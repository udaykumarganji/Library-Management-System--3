# Library-Management-System--3


# Task 3 – Writing Basic SELECT Queries

##  Overview
This task involves practicing basic `SELECT` queries to extract and filter data from the library database. It includes filtering, sorting, limiting, aliasing, and pattern matching.

##  Tools Used
- MySQL Workbench / DB Browser for SQLite
- GitHub for version control

## Description

###  SQL Features Demonstrated
- `SELECT *` to retrieve all columns
- `SELECT column1, column2` to retrieve specific columns
- `WHERE`, `AND`, `OR`, `LIKE`, `BETWEEN` for filtering
- `ORDER BY` for sorting results
- `LIMIT` to restrict the number of output rows
- `DISTINCT` to retrieve unique values
- Aliasing using `AS`
- Joins with the `Categories` table

###  Files Included
- `task3_queries.sql` – SQL script with SELECT queries
- `README.md` – This file

### 🔍 Example Queries
- Books containing the word ‘Harry’
- Books published between 1930 and 2000
- Sorting by published year in descending order
- Books in the 'Fiction' category using JOIN

##  How to Use
1. Load the database schema and data from Tasks 1 & 2.
2. Run `task3_queries.sql` in your SQL tool.
3. Observe and analyze query results.


